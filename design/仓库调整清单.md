# 仓库调整清单

> 记录仓库后台 WMS 与仓配移动端 H5 的已拍板调整项。未拍板建议不写入本文件。

## 2026-06-22 v1 仓库后台 WMS 与仓配移动端 H5 边界拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 1 | P0 | 数据清理工具不进入 WMS 正常功能 | `数据清理工具` 不放在 WMS 正式菜单。只允许测试或开发环境内部使用，生产环境仓库角色不可见，避免误删业务数据。 | WMS 基础与系统管理、权限管理 |
| 2 | P0 | 盘点差异不静默自动调整库存 | 盘点差异审核后生成库存调整单，二次确认后才生效，并写库存流水。不要在审核后静默自动按 FIFO 改库存。 | 库存管理、即时盘点、库存流水 |
| 3 | P0 | 补货概念拆分 | 补货拆成三类：仓内补货、采购补货、售后补发。仓内补货指存储区到拣货区；采购补货指供应商或采购单补货；售后补发指订单商品补货。三类入口和状态不要混在一起。 | 补货管理、采购管理、售后补发、仓配 H5 |
| 4 | P0 | WMS 不维护供应商主档 | 供应商资料归运营后台和供应商模块维护。WMS 只读供应商名称、联系人、送货信息和关联送货单。 | WMS 基础数据、供应商/采购协同 |
| 5 | P0 | 仓配 H5 采购员能力收窄 | 仓配 H5 采购员只做采购协同、到仓跟踪、异常确认，不处理采购价格、供应商主档、结算金额。采购金额不在仓库移动端展示。 | 仓配移动端 H5、采购协同、权限管理 |
| 6 | P1 | 收货入库字段第一版不过重 | 收货入库先要求录入实收数量、批次号、生产日期、保质期、质检结果、差异原因和图片凭证。暂不强制上传温度，不做温区校验。 | 收货管理、入库差异、仓配移动端 H5 |

### 本次边界

- 不修改 `design/商城项目UI设计.html`。
- 不修改 `design/商城项目功能清单.md`。
- 本次只记录已确认的 1-5 和修正版 6。
- 温度上传、温区校验、冷链强管控第一版暂不做。

## 2026-06-22 v2 仓库履约闭环拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 7 | P1 | 波次下放失败回滚 | 一键下放前增加库存锁定校验、重复下放校验、FIFO 批次预览和下放批次日志。下放失败时回滚本批次已生成的拣货任务和出库占用，避免半成功状态。 | 出库单与波次下放、拣货任务、库存锁定 |
| 8 | P1 | 拣货缺货二次找货 | 拣货员标记缺货前，先提示同 SKU 其他库位、同批次库存、相邻库位和仓管复核。确认无货后再生成缺货异常，避免人为少找导致缺货率虚高。 | 拣货管理、缺货异常、库存查询 |
| 9 | P1 | 复检装车硬门槛 | 复检未通过不能打印箱标、不能装车；装车必须绑定线路、车辆、司机和箱码。装车完成后才能确认出库，确认出库后司机才允许发车。 | 复检管理、装车管理、司机配送 |
| 10 | P1 | 司机签到留证据 | 保留“司机签到即默认自提点签收”的规则，但签到必须记录定位、时间和交接备注。拒收、门店未开、地址异常、少件破损要单独上报异常。 | 司机配送、团点签到、配送异常 |

### 本次边界

- 不新增温度上传和温区校验。
- 不增加自提点强制签收环节。
- 本次只记录已确认的 7-10。

## 2026-06-22 v3 库存闭环与移动端管理拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 11 | P1 | 退货交接补库存去向 | 退货回仓后必须按商品行选择去向：可售入库、异常库存、报损、退供。每行关联售后单、责任方、图片凭证和库存流水，避免退货只记录数量、不知道最终库存怎么处理。 | 退货交接、退货入库、库存流水、售后责任 |
| 12 | P1 | 库存批次状态补齐 | 批次状态增加：可售、锁定、冻结、异常、报损。临期、质检异常、破损、过期批次默认进冻结或异常库存，审核后再解冻、报损或退供。暂不纳入温度和温区规则。 | 库存管理、批次管理、库存异常 |
| 13 | P2 | WMS 日志中心显式进菜单 | 在“仓库基础/系统管理”下展示“日志中心”，用于查询收货、拣货、装车、盘点、库存流水和异常处理记录。 | WMS 基础与系统管理、日志中心 |
| 14 | P2 | 仓配 H5 增加交接班规则 | 未完成的收货、拣货、复检、盘点任务允许仓库主管转派或交接班。必须记录原执行人、新执行人、转交原因、转交时间和任务当前状态。 | 仓配移动端 H5、权限管理、操作日志 |
| 15 | P2 | 司机今日任务和历史任务分开 | 司机 H5 默认只展示当天待处理路线。历史、已关闭、跨日期配送单放到历史查询，避免司机误点旧单、误签到、误确认退货。 | 司机 H5、配送任务、历史查询 |

### 本次边界

- 第 12 条不纳入温度和温区强管控。
- 本次只记录已确认的 11-15。
- 继续保持只记录拍板，不修改 UI 原型和功能清单。

## 2026-06-22 v4 入库差异、上架与配送衔接拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 16 | P1 | 差异入库与结算口径 | 允许差异入库。收货时按实际到货数量、破损数量、拒收数量记录结果，系统生成差异记录，但不阻断入库完成。供应商可结算数量以实际入库数量为准，不以采购单数量或供应商送货数量为准。少货、破损、规格不符等差异进入后续对账、追责或补货处理。 | 收货入库、入库差异、供应商对账结算 |
| 17 | P1 | 上架确认与库位绑定 | 收货完成不等于可售入库。收货后先进入待上架库存，完成上架确认并绑定库位后，才进入在库或可售库存。 | 收货入库、上架管理、库位管理、库存可售 |
| 18 | P1 | 出库单与司机配送解耦 | 司机配送任务可提前生成和查看，但司机发车必须依赖出库完成。出库异常时配送任务显示待处理，不允许司机绕过出库发车。 | 出库单、装车管理、司机配送 |
| 19 | P2 | 仓库主管工作台待办归类 | 仓库主管工作台按待收货、待上架、待拣货、待复检、待装车、待盘点、待处理异常分组，避免所有任务混在一个列表。 | WMS 工作台、仓库主管待办、任务管理 |
| 20 | P2 | 移动端异常上报字段统一 | 收货、拣货、复检、装车、退货、盘点的异常上报统一字段：异常类型、商品/SKU、数量、原因、图片、责任环节、关联单据。 | 仓配移动端 H5、异常中心、操作日志 |

### 本次边界

- 第 16 条明确允许差异入库。
- 供应商可结算数量以实际入库数量为准。
- 本批仍不要求上传温度，不考虑温区规则。
- 继续保持只记录拍板，不修改 UI 原型和功能清单。

## 2026-06-22 v5 扫码、库存调整与回仓异常拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 21 | P1 | 扫码动作统一 | 收货、上架、拣货、复检、装车、退货回仓尽量统一支持扫商品码、箱码、库位码、任务码。扫码失败时才允许手动搜索，减少移动端误操作。 | 仓配移动端 H5、收货、上架、拣货、复检、装车、退货回仓 |
| 22 | P1 | 库存调整权限与留痕 | 盘点差异、报损、冻结、解冻、库存调整允许同一人发起并审核，不强制双人复核。但必须保留完整留痕：调整前库存、调整后库存、调整原因、图片凭证、关联单据、操作人、审核人、操作时间。后续通过日志中心和异常报表追溯责任。 | 库存管理、库存调整、日志中心、异常报表 |
| 23 | P1 | 拣货任务按路线和波次分组 | 仓配 H5 拣货任务不要只按商品平铺，支持按波次、线路、自提点、商品维度切换，方便仓库按实际作业方式执行。 | 拣货任务、波次管理、线路配送、仓配移动端 H5 |
| 24 | P2 | 装车少件拦截 | 装车扫码时发现箱码缺失、重复、线路不匹配、自提点不匹配，要直接拦截并生成装车异常，不能只做提示。 | 装车管理、箱码管理、配送线路、异常中心 |
| 25 | P2 | 司机回仓异常闭环 | 司机带回的拒收、破损、少件、错配商品，需要在 H5 有回仓交接入口，仓库确认后再进入退货、报损、补发或异常库存处理。 | 司机 H5、回仓交接、退货入库、异常库存 |

### 本次边界

- 第 22 条允许同一人发起并审核，不强制双人复核。
- 库存调整类操作必须完整留痕，后续通过日志中心和异常报表追责。
- 本批仍不要求上传温度，不考虑温区规则。
- 继续保持只记录拍板，不修改 UI 原型和功能清单。

## 2026-06-22 v6 FIFO、缺货状态与弱网保护拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 26 | P1 | 批次先进先出可解释 | 出库、拣货、库存调整涉及 FIFO 时，WMS 要能展示“为什么扣这个批次”：批次号、入库时间、库位、可用数量、锁定数量，方便仓管复核。 | 出库管理、拣货任务、库存调整、批次库存 |
| 27 | P1 | 缺货作为正常履约状态联动 | 存在缺货是仓库履约里的正常状态，不走异常流程，也不生成异常处理单。拣货确认缺货后，同步更新对应订单、自提点汇总和运营后台履约状态，让运营能看到哪些订单/商品缺货、缺多少、是否需要退款、补货或替换。缺货可以有状态、记录和统计，但不按异常流程处理。 | 拣货管理、订单履约、自提点汇总、运营后台 |
| 28 | P1 | 波次取消与重算规则 | 波次下放后如果发现线路、库存、商品数量异常，允许仓库主管取消未开始的波次并重算；已开始拣货的波次只能走状态处理和人工调整，不直接重算覆盖。 | 波次管理、拣货任务、仓库主管工作台 |
| 29 | P2 | H5 弱网保护 | 仓配 H5 扫码、提交、拍照上传遇到弱网时，要有明确的提交中、失败重试、已暂存状态，避免员工重复提交或误以为成功。 | 仓配移动端 H5、扫码作业、图片上传 |
| 30 | P2 | 仓库绩效只做过程统计 | 第一版只统计仓库作业过程：收货量、拣货量、复检量、装车量、缺货量、超时任务、任务完成时长。不做司机费用，不做薪酬，不做罚款计算。 | WMS 报表、仓库绩效、作业统计 |

### 本次边界

- 第 27 条明确缺货是正常履约状态，不走异常流程。
- 第 30 条不做司机费用、薪酬和罚款计算。
- 本批仍不要求上传温度，不考虑温区规则。
- 继续保持只记录拍板，不修改 UI 原型和功能清单。

## 2026-06-22 v7 收货质检、上架联动与复检范围拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 31 | P1 | 收货质检结果分流 | 收货质检结果分成：合格入库、部分合格入库、拒收入库外处理、待确认。只有合格和部分合格数量进入实际入库数量，拒收数量只记录差异，不进入可结算数量。 | 收货入库、质检结果、入库差异、供应商结算 |
| 32 | P1 | 上架任务和库存状态联动 | 上架任务未完成前，库存状态保持待上架；上架完成后才变为在库或可售。如果上架数量小于收货数量，要记录差异去向：继续待上架、异常库存、报损、退供。 | 上架管理、库存状态、库位管理、库存流水 |
| 33 | P2 | 拣货路径第一版轻量处理 | 第一版不做复杂路径算法，先按仓区、通道、库位编码排序展示拣货任务，让员工少走回头路。后续如果仓库规模变大，再考虑路径优化。 | 拣货任务、仓配移动端 H5、库位管理 |
| 34 | P1 | 第一版先不做复检功能 | 第一版不做复检模块，也不设计复检差异回拣机制。出库履约先按“拣货完成 -> 装车 -> 确认出库”推进。少件、错件、串货等问题先在拣货或装车环节处理：能补齐的继续履约，补不齐的同步为缺货状态。缺货仍按正常履约状态处理，不走异常流程。 | 出库履约、拣货管理、装车管理、缺货状态 |
| 35 | P1 | 出库确认反写库存和履约 | 装车完成不等于库存最终扣减。以“确认出库”为库存正式扣减和订单履约状态推进节点，确认后同步更新订单、自提点汇总、司机配送任务。 | 出库管理、库存扣减、订单履约、司机配送 |

### 本次边界

- 第一版先不做复检功能，前序拍板中涉及复检的内容统一顺延，不作为第一版范围。
- 出库履约第一版按“拣货完成 -> 装车 -> 确认出库”推进。
- 缺货仍按正常履约状态处理，不走异常流程。
- 本批仍不要求上传温度，不考虑温区规则。
- 继续保持只记录拍板，不修改 UI 原型和功能清单。

## 2026-06-22 v8 库存锁定、装车交接与作业看板拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 36 | P1 | 库存锁定与释放规则 | 波次生成后锁定库存；波次取消、拣货缺货、出库取消时要释放对应锁定量。确认出库后才从锁定库存转为正式扣减，避免库存长期被占用。 | 波次管理、库存锁定、出库管理、库存流水 |
| 37 | P1 | 装车按线路交接清单 | 装车完成后生成线路交接清单，包含线路、自提点、箱数、商品汇总、司机、车辆、装车人、装车时间。司机 H5 只接收已装车完成的线路任务。 | 装车管理、线路交接、司机 H5、配送任务 |
| 38 | P1 | 拣货缺货后库存不自动归零 | 拣货员报缺货只影响本次任务和对应订单履约状态，不自动把该 SKU 库存清零。库存是否调整仍走盘点、库存调整或上架核查。 | 拣货管理、缺货状态、库存调整、盘点管理 |
| 39 | P2 | 仓配 H5 任务领取方式 | 第一版支持系统分配和主管转派，普通员工不做自由抢单，避免多人同时处理同一任务。任务开始、暂停、完成都要留时间记录。 | 仓配移动端 H5、任务管理、主管工作台、操作日志 |
| 40 | P2 | WMS 看板先做作业监控 | WMS 看板第一版只展示待收货、待上架、待拣货、待装车、已出库、缺货、库存冻结/异常、超时任务，不做复杂经营分析。 | WMS 看板、作业监控、库存监控 |

### 本次边界

- 第一版继续不做复检功能。
- 缺货仍按正常履约状态处理，不走异常流程。
- 不做司机费用、薪酬和罚款计算。
- 本批仍不要求上传温度，不考虑温区规则。
- 继续保持只记录拍板，不修改 UI 原型和功能清单。

## 2026-06-22 v9 仓库 UI 原型落地记录

| 编号 | 优先级 | 调整项 | 已落地 UI | 影响范围 |
| --- | --- | --- | --- | --- |
| 41 | P0 | WMS 菜单收口 | 仓库后台 WMS 菜单保留仓库单据流转、采购管理、收货管理、出库单与波次、拣货装车出库、司机配送、车辆装载、地图线路、退货交接、库存盘点补货、水果仓专区、日志中心和作业统计；不放数据清理工具，不展示司机费用菜单。 | `design/商城项目UI设计.html` / 仓库后台 WMS |
| 42 | P0 | 出库履约去复检 | WMS 和仓配 H5 出库链路统一展示“拣货完成 -> 装车 -> 确认出库”，装车员角色只做箱标、扫码装车、线路交接、确认出库；复检仅作为历史内部函数名保留，不作为第一版页面能力展示。 | WMS 拣货装车出库、仓配 H5 装车员 |
| 43 | P0 | 缺货状态改为履约记录 | 仓配 H5 拣货员缺货标记改为“记录履约缺货”，说明缺货同步订单、自提点汇总和运营履约状态，不生成异常处理单，不自动清零库存。 | 仓配 H5 拣货员、WMS 出库单 |
| 44 | P0 | 收货温度弱化 | 收货员 H5 质检展示保留存储建议和质检备注，温度输入改为非必填；第一版不强制上传温度，不做温区校验。 | 仓配 H5 收货员、WMS 收货管理 |
| 45 | P0 | 司机费用改作业统计 | 司机 H5 我的页入口改为作业统计，统计装车量、配送点、配送件数、退货取回、超时任务和完成状态；规则弹窗明确不展示司机费用、薪酬、罚款、补贴和结算金额。 | 仓配 H5 司机、WMS 作业统计 |
| 46 | P1 | 采购员金额隐藏 | 采购员 H5 继续只展示采购协同、到仓跟踪、异常确认、商品数量和入库回执，不展示采购价格、供应商主档和结算金额。 | 仓配 H5 采购员 |

### 本次边界

- 本次允许同步 UI 原型，覆盖前序“只记录拍板，不修改 UI 原型”的旧边界。
- 只调整仓库后台 WMS 与仓配移动端 H5。
- 不调整运营后台其他模块、用户小程序、自提点小程序和供应商小程序。
- 未写业务代码、SQL、接口和后端实现。

## 2026-06-22 v10 收货来源、采购单入库与司机可见信息拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 47 | P1 | 收货任务来源收口 | WMS 收货任务优先来自采购单或送货单，不让仓库手工随意新建无来源入库。临时到货可以登记，但必须后续关联采购单或由采购确认。 | 收货管理、采购协同、送货单 |
| 48 | P1 | 采购单一次到仓入库 | 同一采购单第一版只允许一次到仓、一次收货入库，不考虑多次到仓、多次入库。收货时记录采购单数量、实收数量、差异数量、批次、收货人、收货时间。未到货、少货、破损、拒收等都在本次收货结果里体现，后续通过差异记录、对账、补货或采购关闭处理，不做分批挂起。 | 采购单、收货入库、入库差异、供应商对账 |
| 49 | P1 | 移库第一版轻量处理 | 第一版支持库位间移库，记录原库位、目标库位、商品/SKU、数量、操作人、原因。不做复杂补货策略，只保证库存位置准确。 | 移库管理、库位库存、库存流水 |
| 50 | P1 | 装车撤销规则 | 装车完成后、确认出库前，允许仓库主管撤销装车或调整箱码；确认出库后不允许直接撤销，只能走回仓、退货、缺货或补发处理。 | 装车管理、箱码管理、确认出库、回仓交接 |
| 51 | P2 | 司机任务只看执行信息 | 司机 H5 只展示线路、自提点、箱数、装车状态、签到状态、交接备注，不展示采购价、供应商结算、仓库绩效等后台信息。 | 司机 H5、配送任务、权限控制 |

### 本次边界

- 原计划 41-45 编号因当前文件已有 v9 记录占用，顺延为 47-51。
- 第 48 条明确同一采购单第一版只允许一次到仓、一次收货入库。
- 第一版继续不做复检功能。
- 缺货仍按正常履约状态处理，不走异常流程。
- 不做司机费用、薪酬和罚款计算。
- 本批仍不要求上传温度，不考虑温区规则。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v11 采购单关闭、临时到货与角色首页拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 52 | P1 | 收货完成后采购单状态关闭 | 同一采购单完成一次收货入库后，采购单进入已收货或已关闭状态。少货、破损、拒收不再挂起等二次到货，而是进入差异、对账、补货或采购关闭处理。 | 采购单、收货入库、入库差异、采购协同 |
| 53 | P1 | 临时到货不直接入可售库存 | 临时到货如果还没关联采购单或采购确认，只能进入待确认库存，不进入可售库存，也不进入供应商可结算数量。 | 临时到货、待确认库存、供应商结算、库存状态 |
| 54 | P1 | 移库不改变库存归属 | 库位间移库只改变库存所在库位，不改变批次、库存状态、可售数量和结算口径。冻结、异常、报损库存不能通过移库变成可售。 | 移库管理、库位库存、批次库存、库存状态 |
| 55 | P2 | 装车交接清单可追溯 | 线路交接清单要能追溯每个箱码的装车人、装车时间、司机、车辆、线路、自提点。后续司机签到、回仓交接都能关联这张清单。 | 装车管理、线路交接、司机签到、回仓交接 |
| 56 | P2 | 仓配 H5 首页按角色聚合 | 仓配 H5 首页按角色只展示当前角色的待办任务和统计，不展示无权限模块。收货员、拣货员、装车员、司机、采购员看到的入口要分开。 | 仓配移动端 H5、角色权限、任务首页 |

### 本次边界

- 同一采购单仍然只允许一次到仓、一次收货入库。
- 临时到货未确认前不进入可售库存，也不进入供应商可结算数量。
- 第一版继续不做复检功能。
- 缺货仍按正常履约状态处理，不走异常流程。
- 不做司机费用、薪酬和罚款计算。
- 本批仍不要求上传温度，不考虑温区规则。
- 本次只同步仓库后台 WMS 与仓配移动端 H5 的 UI 原型，不修改其他端、功能清单、业务代码、SQL、接口和后端实现。

## 2026-06-22 v12 差异记录、待确认库存与日志串联拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 57 | P1 | 采购关闭后的差异处理 | 采购单一次收货后即关闭。少货、破损、拒收、规格不符只进入本次收货差异记录，用于采购确认、供应商对账、补货判断，不再反向打开原采购单。 | 采购单、收货入库、差异记录、供应商对账 |
| 58 | P1 | 待确认库存处理提醒 | 临时到货、无采购单到货、信息不匹配商品进入待确认库存。第一版只做待确认状态和处理提醒，超时提醒仓库主管和采购员。 | 待确认库存、临时到货、采购协同、仓库主管工作台 |
| 59 | P1 | 装车前箱码完整性校验 | 装车前按线路校验箱码是否齐全、是否重复、是否属于当前线路和自提点。校验不通过不能确认装车完成。 | 装车管理、箱码管理、线路交接 |
| 60 | P2 | 司机签到不改变库存 | 司机到自提点签到只记录配送履约进度、定位、时间和交接备注，不影响仓库库存。库存扣减仍以确认出库为准。 | 司机 H5、配送签到、库存扣减、订单履约 |
| 61 | P2 | 仓库日志按单据串联 | 日志中心要能按采购单、收货单、入库批次、波次、出库单、箱码、司机线路串联查看，不只按操作时间流水展示。 | 日志中心、采购单、收货单、出库单、司机线路 |

### 本次边界

- 第一版不做异常数据模块，不做异常数据池，不做异常数据分析。
- 少货、破损、拒收、规格不符只进入本次收货差异记录。
- 待确认库存只做状态和处理提醒。
- 同一采购单仍然只允许一次到仓、一次收货入库。
- 第一版继续不做复检功能。
- 缺货仍按正常履约状态处理，不走异常流程。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v13 回仓来源、盘点差异与移动端防重复拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 62 | P1 | 回仓交接必须有来源 | 司机带回商品、拒收商品、错配商品进入回仓交接时，必须关联原线路、原自提点、原出库单或原箱码。不允许仓库直接做无来源回仓入库。 | 回仓交接、司机 H5、出库单、箱码管理 |
| 63 | P1 | 第一版先不做报损报溢 | 第一版不做报损、报溢功能，也不设计报损/报溢审批流。破损、过期、规格不符、实物多出等情况先只记录在收货差异、盘点差异、待确认库存或退货回仓结果里，不直接生成报损单或报溢单，不把库存自动转成报损/报溢状态。 | 库存管理、收货差异、盘点差异、待确认库存、回仓交接 |
| 64 | P1 | 盘点任务只记录差异 | 盘点第一版按仓库、库区、库位、SKU 维度发起任务。盘点中记录账面数量、实盘数量、差异数量、盘点人、盘点时间。盘点差异先只记录和审核，不做报损/报溢流程；需要调整库存时走库存调整留痕。 | 盘点管理、库存调整、库存流水、日志中心 |
| 65 | P2 | 箱码生成节点收口 | 第一版不做复检后生成箱码。在拣货完成后生成箱码，装车环节扫码校验线路、自提点、箱数和箱码状态。 | 拣货管理、箱码管理、装车管理 |
| 66 | P2 | 移动端提交防重复 | 仓配 H5 的确认入库、确认上架、确认拣货、确认装车、确认出库、回仓交接等关键动作，要有提交中、防重复点击、成功/失败反馈，避免弱网下重复作业。 | 仓配移动端 H5、弱网保护、操作日志 |

### 本次边界

- 第一版先不做报损、报溢功能和审批流。
- 盘点差异只记录和审核，需要调整库存时走库存调整留痕。
- 第一版继续不做复检功能。
- 不做异常数据模块、异常数据池和异常数据分析。
- 缺货仍按正常履约状态处理，不走异常流程。
- 同一采购单仍然只允许一次到仓、一次收货入库。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v14 可售库存、箱码状态与 H5 回看拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 67 | P1 | 待上架库存不可出库 | 收货完成后进入待上架库存，未完成上架和库位绑定前，不允许被波次锁定、拣货或出库。 | 收货入库、上架管理、库存锁定、出库管理 |
| 68 | P1 | 可售库存口径统一 | WMS 可售库存 = 已上架在库数量 - 锁定数量 - 冻结数量。待上架、待确认、差异记录、回仓未确认数量都不进入可售库存。 | 库存管理、可售库存、波次锁定、待确认库存 |
| 69 | P1 | 箱码状态流转 | 箱码第一版状态为：已生成、已装车、已出库、已签收、已回仓。装车、确认出库、司机签到、回仓交接分别推进状态。 | 箱码管理、装车管理、司机签到、回仓交接 |
| 70 | P2 | 仓库主管可强制结束任务 | 任务卡死、员工误操作、弱网重复提交时，仓库主管可以强制结束或重置任务，但必须填写原因，并记录操作前后状态。 | 主管工作台、任务管理、操作日志 |
| 71 | P2 | H5 操作结果可回看 | 收货员、拣货员、装车员、司机完成关键动作后，H5 要能查看自己的最近操作结果，避免不知道刚才有没有提交成功。 | 仓配移动端 H5、操作记录、弱网保护 |

### 本次边界

- 待上架、待确认、差异记录、回仓未确认数量不进入可售库存。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 缺货仍按正常履约状态处理，不走异常流程。
- 同一采购单仍然只允许一次到仓、一次收货入库。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v15 冻结库存、库存流水与出库取消拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 72 | P1 | 冻结库存来源明确 | 冻结库存必须有来源：采购确认、质检待确认、库存调整、盘点差异审核、系统锁定。冻结只影响可售，不直接改变实物数量。 | 库存管理、冻结库存、可售库存、库存调整 |
| 73 | P1 | 库存流水不可缺字段 | 每次入库、上架、移库、锁定、释放、出库、回仓、库存调整都要写库存流水，字段至少包含单据来源、SKU、批次、库位、变动前数量、变动数量、变动后数量、操作人、时间。 | 库存流水、入库、上架、移库、出库、回仓 |
| 74 | P1 | 出库取消释放链路 | 出库单取消时，要同步释放波次锁定量、箱码状态、装车状态和司机任务状态。已确认出库的单据不允许直接取消，只能走回仓或后续履约处理。 | 出库管理、波次锁定、箱码管理、司机任务 |
| 75 | P2 | 仓配 H5 只保留当前任务入口 | 移动端员工首页默认只展示自己当前可执行任务，历史任务、已完成任务放到查询页，减少误点旧任务。主管可看全部任务和转派入口。 | 仓配移动端 H5、任务首页、历史查询、主管工作台 |
| 76 | P2 | WMS 筛选条件按作业场景收口 | WMS 列表筛选第一版优先保留仓库、日期、单据号、SKU、状态、线路、自提点、操作人，不做过多高级筛选，避免页面过重。 | WMS 列表、筛选条件、作业查询 |

### 本次边界

- 冻结库存只影响可售，不直接改变实物数量。
- 已确认出库的单据不允许直接取消，只能走回仓或后续履约处理。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 缺货仍按正常履约状态处理，不走异常流程。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v16 上架差异、波次校验与日志拆分拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 77 | P1 | 上架差异不直接改库存 | 上架时发现实物数量少于待上架数量，只记录上架差异和实际上架数量，不直接静默改库存。需要调整时走库存调整留痕。 | 上架管理、库存调整、库存流水、操作日志 |
| 78 | P1 | 波次生成前校验可售库存 | 波次生成前必须校验可售库存、锁定库存、待上架库存、待确认库存。只有可售库存可被波次锁定，避免把不可出库库存拉进履约。 | 波次管理、可售库存、库存锁定、出库管理 |
| 79 | P1 | 回仓确认后库存去向收口 | 回仓确认后第一版只允许进入待确认库存或可售库存。是否可售由仓库确认，不做报损、报溢、异常数据流转。 | 回仓交接、待确认库存、可售库存、库存状态 |
| 80 | P2 | 司机线路任务状态简化 | 司机线路任务状态第一版只保留：待发车、配送中、已完成、已回仓。不要设计复杂中间状态，减少司机误操作。 | 司机 H5、线路任务、配送状态 |
| 81 | P2 | WMS 操作日志和库存流水分开 | 操作日志记录谁做了什么动作；库存流水记录库存数量如何变化。两者可以互相关联，但不要混成一个列表。 | 日志中心、库存流水、操作日志 |

### 本次边界

- 上架差异只记录差异和实际上架数量，不静默改库存。
- 波次只能锁定可售库存。
- 回仓确认后第一版只进入待确认库存或可售库存。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 缺货仍按正常履约状态处理，不走异常流程。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v17 批次收口、库位校验与缺货处理拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 82 | P1 | 批次信息第一版收口 | 批次只保留批次号、生产日期、保质期、入库数量、实际上架数量、库存状态、库位。不做温区、温度、复杂批次属性。 | 批次库存、收货入库、上架管理、库存状态 |
| 83 | P1 | 上架库位校验 | 上架时校验库位是否属于当前仓库、是否可用、是否已停用。库位错误时不允许确认上架，避免库存挂到不存在或停用库位。 | 上架管理、库位管理、库存准确性 |
| 84 | P1 | 波次优先级人工控制 | 第一版波次优先级由仓库主管手动选择或调整，参考配送日期、线路、自提点、订单量。不做复杂自动波次优化算法。 | 波次管理、仓库主管工作台、出库履约 |
| 85 | P1 | 缺货后订单处理留给运营 | 仓库只记录缺货商品、缺货数量、影响订单和自提点汇总，不在 WMS 自动退款，也不处理替换商品。第一版先不做替换商品功能。缺货后的退款、补货、订单处理由运营后台决定；仓库只提供缺货记录和履约影响信息。 | 缺货状态、订单履约、自提点汇总、运营后台 |
| 86 | P2 | 仓配 H5 任务卡片信息收口 | 移动端任务卡片只展示执行必需信息：任务类型、单据号、SKU/商品、数量、库位/线路、自提点、状态、截止时间。不展示结算、成本、绩效等信息。 | 仓配移动端 H5、任务卡片、权限展示 |

### 本次边界

- 第一版先不做替换商品功能。
- 缺货后的退款、补货、订单处理由运营后台决定。
- 批次不做温区、温度、复杂批次属性。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v18 差异原因、出库状态与扫码兜底拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 87 | P1 | 收货差异原因标准化 | 收货差异原因第一版收口为：少货、破损、拒收、规格不符、待采购确认。只用于差异记录和对账依据，不进入异常数据模块。 | 收货入库、差异记录、供应商对账、采购协同 |
| 88 | P1 | 上架完成反写批次数量 | 上架完成后要反写批次的实际上架数量、库位、库存状态。未上架数量继续停留在待上架，不进入可售库存。 | 上架管理、批次库存、可售库存、库存状态 |
| 89 | P1 | 出库单状态简化 | 出库单第一版状态只保留：待波次、已锁定、拣货中、待装车、已装车、已出库、已取消。不要加入复检状态。 | 出库单、波次管理、拣货管理、装车管理 |
| 90 | P2 | 主管改派任务留痕 | 仓库主管改派收货、上架、拣货、装车、回仓任务时，要记录原执行人、新执行人、改派原因、改派时间。 | 主管工作台、任务改派、操作日志 |
| 91 | P2 | 移动端扫码失败兜底 | H5 扫码失败时允许手动输入或搜索单据/箱码/库位码，但必须记录为手动处理，便于后续排查误操作。 | 仓配移动端 H5、扫码作业、操作日志 |

### 本次边界

- 收货差异原因只用于差异记录和对账依据，不进入异常数据模块。
- 出库单状态不加入复检状态。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 缺货仍按正常履约状态处理，不走异常流程。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-22 v19 待上架任务、拣货口径与回仓分工拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 92 | P1 | 待上架任务自动生成 | 收货完成后，按商品/SKU、批次、建议库位生成待上架任务。仓库员工只能处理已分配或主管转派的上架任务，避免无任务直接改库位库存。 | 收货入库、上架管理、主管工作台、库位库存 |
| 93 | P1 | 拣货完成口径明确 | 拣货完成不等于出库完成。拣货完成只代表商品已从库位拣出并生成箱码/待装车记录，库存仍处于锁定状态，直到确认出库才正式扣减。 | 拣货管理、箱码管理、装车管理、库存锁定 |
| 94 | P1 | 装车确认前二次校验 | 确认装车前校验线路、自提点、箱码、商品数量、司机、车辆是否一致。校验不通过不能完成装车，但不进入异常数据模块，只停留在待处理状态。 | 装车管理、箱码校验、司机任务、车辆管理 |
| 95 | P2 | 回仓任务和司机任务分离 | 司机 H5 只发起回仓交接，仓库端确认回仓任务。司机不能直接决定商品进入可售或待确认库存，库存去向由仓库确认。 | 司机 H5、回仓交接、库存状态、仓库确认 |
| 96 | P2 | WMS 首页数字可点击下钻 | WMS 首页的待收货、待上架、待拣货、待装车、缺货、待确认库存、冻结库存数字都要能点击进入对应列表，避免看板只展示不能处理。 | WMS 首页、作业看板、任务列表、库存列表 |

### 本次边界

- 拣货完成不等于出库完成，确认出库才正式扣减库存。
- 装车校验不通过时不进入异常数据模块，只停留在待处理状态。
- 回仓库存去向由仓库确认，司机不能直接决定。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v20 入库、上架、装车回执与操作记录拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 97 | P1 | 收货入库成功回执 | 收货完成后生成入库回执，展示采购单号、实收数量、差异数量、批次、待上架数量、收货人、时间。采购员 H5 和 WMS 都能查看。 | 收货入库、采购员 H5、WMS 收货管理、操作记录 |
| 98 | P1 | 上架完成回执 | 上架完成后生成上架回执，展示批次、库位、实际上架数量、待上架剩余数量、上架人、时间，便于仓库主管追踪库存落位。 | 上架管理、批次库存、主管工作台、操作记录 |
| 99 | P1 | 装车完成回执 | 装车完成后生成装车回执，展示线路、自提点、箱数、箱码、司机、车辆、装车人、时间。司机 H5 接收的线路任务以这张回执为依据。 | 装车管理、司机 H5、线路任务、箱码管理 |
| 100 | P2 | 仓库主管任务总览轻量化 | 主管总览第一版按任务状态分组：待收货、待上架、待拣货、待装车、待回仓、待确认库存、已完成。先不做复杂绩效排名。 | 主管工作台、任务总览、作业看板 |
| 101 | P2 | H5 个人操作记录 | 仓配 H5 每个角色保留自己的最近操作记录：收货、上架、拣货、装车、确认出库、回仓交接、司机签到。只看本人记录，主管可看全员。 | 仓配移动端 H5、操作记录、主管工作台 |

### 本次边界

- 本批按确认日期记录为 2026-06-23。
- 第一版先不做复杂绩效排名。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 缺货仍按正常履约状态处理，不走异常流程。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v21 采购员可见范围、角色权限与轻量提醒拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 102 | P1 | 采购员只看入库结果 | 采购员 H5 只查看采购单到仓状态、入库回执、差异数量和待采购确认事项，不进入仓库库存、库位、波次、装车、司机配送等内部作业页面。 | 采购员 H5、收货入库、采购协同、权限控制 |
| 103 | P1 | 仓库角色权限收口 | 收货员、上架员、拣货员、装车员、司机、采购员、仓库主管权限分开。普通角色只能处理自己的任务，主管可查看、改派、强制结束和回看全员记录。 | 仓配移动端 H5、WMS 权限、主管工作台、操作记录 |
| 104 | P1 | 待采购确认事项轻量化 | 待采购确认只用于临时到货、无采购单到货、规格不符、差异数量确认。采购确认后进入差异记录、待确认库存处理或采购关闭，不做异常数据模块。 | 采购协同、待采购确认、差异记录、待确认库存 |
| 105 | P2 | WMS 表格操作按钮收口 | 列表页第一版只保留查看、处理、转派、导出、关闭/取消等常用操作。复杂批量操作、批量报损、批量报溢、批量复检第一版不做。 | WMS 列表、表格操作、任务处理 |
| 106 | P2 | H5 只做轻量消息提醒 | 仓配 H5 第一版只做任务分配、转派、待确认、失败重试等轻量提醒，不做复杂站内消息中心和营销类消息。 | 仓配移动端 H5、消息提醒、任务通知 |

### 本次边界

- 采购员不进入仓库库存、库位、波次、装车、司机配送等内部作业页面。
- 待采购确认不做异常数据模块。
- 第一版不做复杂批量操作、批量报损、批量报溢、批量复检。
- H5 只做轻量任务提醒，不做复杂站内消息中心和营销类消息。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 第一版先不做替换商品功能。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v22 一线金额可见、出库权限与 H5 状态拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 107 | P1 | 一线角色不看金额 | 收货员、上架员、拣货员、装车员、司机 H5 均不展示采购价、销售价、结算金额、供应商对账金额。金额类只留给运营、采购、财务后台。 | 仓配移动端 H5、WMS 权限、采购协同、财务对账 |
| 108 | P1 | 出库确认权限收口 | 确认出库只能由仓库主管或具备出库权限的装车员执行。拣货员和司机不能确认出库，司机只能看到已出库后的线路任务。 | 出库管理、装车管理、司机 H5、权限控制 |
| 109 | P1 | 待确认库存处理结果收口 | 待确认库存第一版处理结果只保留：确认可售、继续待确认、采购关闭。先不做报损、报溢、退供、异常流转。 | 待确认库存、采购协同、库存状态、WMS 库存管理 |
| 110 | P2 | 导出功能第一版收口 | WMS 第一版只允许主管导出收货记录、上架记录、拣货记录、装车记录、库存流水、操作日志。H5 不提供导出。 | WMS 列表、导出权限、仓配移动端 H5、操作日志 |
| 111 | P2 | H5 页面空状态和失败状态 | 仓配 H5 每个任务列表都要有空状态、加载失败、提交失败、重试入口，避免员工看到白屏或误以为没有任务。 | 仓配移动端 H5、任务列表、失败重试、移动端体验 |

### 本次边界

- 一线角色不看金额，金额类只留给运营、采购、财务后台。
- 出库确认仅仓库主管或具备出库权限的装车员可执行。
- 待确认库存第一版不做报损、报溢、退供、异常流转。
- H5 不提供导出功能。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 缺货仍按正常履约状态处理，不走异常流程。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v23 任务分配、箱码交接与查询兜底拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 112 | P1 | H5 任务领取方式收口 | 仓配 H5 第一版不做员工自由抢单。任务由系统生成后分配给角色或由主管转派，员工只处理自己名下任务。 | 仓配 H5、任务分配、主管工作台 |
| 113 | P1 | 任务撤回和转派留痕 | 主管撤回、转派任务时必须记录原处理人、新处理人、原因、时间。普通员工不能删除任务。 | WMS 权限、任务流转、操作日志 |
| 114 | P1 | 箱码作为出库交接主线 | 拣货完成后按箱码进入待装车，装车、出库、司机交接、自提点收货都围绕箱码核对，不按散乱商品明细人工对账。 | 拣货、装车、出库、司机 H5、自提点收货 |
| 115 | P2 | H5 扫码失败兜底 | 扫码失败时允许手动输入箱码、库位码、商品条码，但必须记录为手工录入，并进入操作日志。 | 仓配 H5、扫码、操作日志 |
| 116 | P2 | WMS 查询条件标准化 | WMS 收货、上架、拣货、装车、库存流水列表统一支持按仓库、状态、单号、商品、人员、时间范围查询。第一版不做高级自定义筛选器。 | WMS 列表页、查询筛选、数据追踪 |

### 本次边界

- 第一版不做员工自由抢单。
- 普通员工不能删除任务。
- 箱码作为出库、装车、司机交接的主线。
- 扫码失败可以手工录入，但必须留痕。
- 第一版不做高级自定义筛选器。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 不做司机费用、薪酬、罚款。
- 不要求上传温度，不考虑温区规则。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v24 库存来源、冻结口径与 H5 防重复拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 117 | P1 | 库位库存变动必须有来源单据 | 库位库存不能手工无来源调整。第一版只允许通过收货上架、拣货锁定、确认出库、回仓确认、采购关闭等业务动作产生库存变动。 | 库位库存、库存流水、WMS 权限 |
| 118 | P1 | 库存流水展示业务口径 | 库存流水必须展示变动类型、来源单号、商品/SKU、批次、库位、变动前后数量、操作人、时间。金额不展示给仓库一线角色。 | 库存流水、批次库存、操作记录 |
| 119 | P1 | 冻结库存第一版来源收口 | 冻结库存只来自波次锁定、拣货锁定、出库待确认、采购关闭待处理。第一版不做人工冻结、手工解冻入口。 | 库存状态、波次、拣货、出库 |
| 120 | P2 | H5 提交动作防重复 | 仓配 H5 的确认收货、确认上架、完成拣货、确认装车、确认出库、回仓交接提交后要禁用按钮并显示提交中，避免重复提交。 | 仓配 H5、任务提交、幂等体验 |
| 121 | P2 | 主管可查看失败提交记录 | WMS 主管端可以查看 H5 提交失败记录，包括任务、人员、失败原因、时间、重试状态。第一版只做排查，不做异常数据模块。 | 主管工作台、H5 提交失败、操作日志 |

### 本次边界

- 不允许无来源手工调整库位库存。
- 冻结库存第一版不做人为冻结和手工解冻入口。
- H5 提交动作要防重复。
- 提交失败记录只用于主管排查，不进入异常数据模块。
- 金额继续不展示给仓库一线角色。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 不做司机费用、薪酬、罚款。
- 不要求上传温度，不考虑温区规则。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v25 H5 弱网、任务状态与主管关闭拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 122 | P1 | H5 弱网提交结果明确 | 仓配 H5 提交后如果网络中断，页面必须展示“提交结果待确认”，员工不能反复点提交。恢复后优先查询任务最新状态。 | 仓配 H5、任务提交、失败重试 |
| 123 | P1 | 任务状态不可随意回退 | 收货、上架、拣货、装车、出库任务状态只能按流程前进。第一版不做普通员工手动回退状态，确需修正由主管关闭后重新分配或重新生成任务。 | WMS 任务流、主管工作台、操作日志 |
| 124 | P1 | 主管关闭任务必须填原因 | 主管关闭收货、上架、拣货、装车、回仓任务时必须填写关闭原因，并记录关闭人、关闭时间。关闭不等于删除，历史仍可查询。 | 主管工作台、任务管理、操作日志 |
| 125 | P2 | H5 任务详情字段统一 | H5 各角色任务详情统一展示任务单号、来源单号、商品/SKU、数量、状态、处理人、创建时间、截止时间。金额、供应商结算信息不展示。 | 仓配 H5、任务详情、权限展示 |
| 126 | P2 | WMS 列表默认只看待处理 | WMS 作业列表默认展示待处理、处理中、待确认数据，已完成和已关闭通过筛选查看，避免一线操作页被历史数据干扰。 | WMS 列表、任务处理、查询筛选 |

### 本次边界

- H5 弱网状态不做离线作业，只做提交结果确认和重试。
- 普通员工不能手动回退任务状态。
- 主管关闭任务要留原因，不能删除历史。
- H5 任务详情继续不展示金额和结算信息。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 不做司机费用、薪酬、罚款。
- 不要求上传温度，不考虑温区规则。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v26 单据追踪、差异原因与误操作防护拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 127 | P1 | 全链路单据关联 | WMS 和 H5 任务详情要能看到来源单据和后续单据，例如采购单、收货单、上架任务、拣货任务、箱码、装车单、出库单、线路任务。 | WMS、仓配 H5、任务详情、单据追踪 |
| 128 | P1 | 数量差异必须留原因 | 收货、上架、拣货、装车时，实际数量与任务数量不一致，必须填写差异原因。只记录差异，不进入异常数据模块。 | 收货、上架、拣货、装车、操作日志 |
| 129 | P1 | 关键完成动作二次确认 | 完成收货、完成上架、完成拣货、确认装车、确认出库、关闭任务等关键动作需要二次确认，避免一线误点。 | 仓配 H5、WMS 操作、任务状态 |
| 130 | P2 | H5 未完成任务优先展示 | 仓配 H5 首页和任务列表默认优先展示本人未完成任务，按待处理、处理中、待确认、已完成排序。历史任务通过筛选查看。 | 仓配 H5、任务列表、首页 |
| 131 | P2 | 仓库权限和切换收口 | 普通仓库员工固定当前仓库，不允许自行切换仓库。仓库主管只能在授权仓库范围内切换查看。 | WMS 权限、仓配 H5、数据隔离 |

### 本次边界

- 数量差异只做记录和原因，不进入异常数据模块。
- 二次确认不是审批流，不增加复核角色。
- 普通员工不能自行切换仓库。
- H5 继续不展示金额和结算信息。
- 第一版继续不做复检功能。
- 第一版继续不做报损、报溢功能和审批流。
- 不做异常数据模块、异常数据池和异常数据分析。
- 第一版先不做替换商品功能。
- 不做司机费用、薪酬、罚款。
- 不要求上传温度，不考虑温区规则。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v27 第一版仓库履约闭环定版收口

### 第一版主链路

采购到仓 → 收货入库 → 待上架 → 上架可售 → 波次锁定 → 拣货 → 箱码待装车 → 装车 → 确认出库 → 司机配送 → 回仓确认 → 库存流水。

### 第一版仓库职责

| 范围 | 定版口径 |
| --- | --- |
| 作业执行 | 仓库负责收货、上架、拣货、装车、确认出库、回仓确认等履约执行动作。 |
| 库存状态 | 仓库负责维护待上架、可售、锁定、冻结、待确认等库存状态，不做无来源手工调整。 |
| 任务流转 | 任务由系统生成或主管转派，一线员工只处理自己名下任务，不做自由抢单。 |
| 箱码交接 | 箱码作为拣货、装车、出库、司机交接、自提点收货的主线。 |
| 差异记录 | 收货、上架、拣货、装车差异必须记录数量和原因，但不进入异常数据模块。 |
| 操作留痕 | 关键动作、转派、关闭、手工录入、失败提交、库存变动都必须进入操作日志或库存流水。 |

### 第一版不归仓库处理

| 范围 | 定版口径 |
| --- | --- |
| 金额结算 | 采购价、销售价、结算金额、供应商对账金额不展示给仓库一线角色。 |
| 订单处理 | 缺货、少货、退款、补货、订单取消等订单侧决策由运营后台处理。 |
| 商品替换 | 第一版不做替换商品功能，仓库只记录缺货和履约影响。 |
| 复杂异常 | 第一版不做异常数据模块、异常数据池、异常数据分析。 |
| 司机费用 | 第一版不做司机费用、薪酬、罚款。 |

### 第一版定版边界

- 同一采购单第一版只允许一次到仓、一次入库。
- 收货允许差异入库，供应商可结算数量以实际入库数量为准。
- 收货完成不等于可售，必须完成上架并绑定库位后才进入可售。
- 波次只锁定可售库存。
- 拣货完成不等于出库完成，确认出库后库存才正式扣减。
- 缺货是正常履约状态，不走异常流程。
- 回仓库存去向由仓库确认，司机不能直接决定。
- 第一版不做复检功能。
- 第一版不做报损、报溢功能和审批流。
- 第一版不做异常数据模块、异常数据池、异常数据分析。
- 第一版不做替换商品功能。
- 第一版不要求上传温度，不考虑温区规则。
- 第一版不做员工自由抢单。
- 第一版不做 H5 导出。
- 第一版不做高级自定义筛选器。
- 第一版不做离线作业，只做弱网提交确认和重试。
- 仓库一线角色不展示金额、采购价、销售价、结算金额、供应商对账金额。

### 后续执行口径

- 后续 UI 和功能清单只围绕本闭环补齐，不再新增第一版范围外能力。
- WMS 和仓配 H5 优先补齐页面状态、字段、权限、操作留痕、单据追踪和失败重试。
- 运营后台负责订单、退款、补货、替换、结算、复杂异常分析等仓库外决策。
- 本次作为第一版仓库履约闭环定版记录，不修改 UI 原型和功能清单。

## 2026-06-23 v28 第一版闭环验收口径拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 132 | P1 | 第一版验收按主链路跑通 | 第一版验收必须能完整跑通：采购到仓 → 收货入库 → 待上架 → 上架可售 → 波次锁定 → 拣货 → 箱码待装车 → 装车 → 确认出库 → 司机配送 → 回仓确认 → 库存流水。任一节点断链都不算定版完成。 | WMS、仓配 H5、验收标准 |
| 133 | P1 | 每个节点必须有状态回写 | 收货、上架、波次、拣货、装车、出库、司机配送、回仓确认完成后，都必须回写状态、操作人、操作时间和来源单据。 | 任务状态、操作日志、单据追踪 |
| 134 | P1 | 页面补齐按角色验收 | WMS 主管、收货员、上架员、拣货员、装车员、司机、采购员 H5 的页面入口、任务列表、任务详情、提交结果、历史记录都要按角色验收。没有角色入口或详情页断层，视为闭环未完成。 | WMS 权限、仓配 H5、角色页面 |
| 135 | P2 | 字段口径统一清单 | 第一版所有任务详情统一字段：任务单号、来源单号、商品/SKU、批次、库位/箱码、数量、状态、处理人、创建时间、完成时间、差异原因。金额和结算字段不进入仓配 H5。 | 任务详情、字段标准、权限展示 |
| 136 | P2 | 闭环问题只分两类处理 | 第一版验收发现的问题只分两类：闭环阻断必须修，体验优化排后续。不要在验收阶段重新打开复检、报损报溢、异常数据模块、替换商品、温区温度、司机费用等范围。 | 验收收口、版本边界、后续排期 |

### 本次边界

- 这是定版后的落地验收收口，不是新增业务范围。
- 第一版验收只看主链路是否跑通、状态是否回写、角色页面是否闭合、字段是否一致。
- 验收阶段不重新打开复检、报损报溢、异常数据模块、替换商品、温区温度、司机费用。
- UI 和功能清单后续只围绕 v27 闭环补齐，不再扩第一版能力。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v29 第一版菜单角色状态收口拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 137 | P1 | 第一版菜单范围锁定 | WMS 第一版菜单只保留闭环所需模块：收货、上架、库存、波次、拣货、装车、出库、回仓、库存流水、操作日志、主管工作台。其他非闭环菜单不进入第一版。 | WMS 菜单、功能清单、UI 收口 |
| 138 | P1 | H5 角色入口锁定 | 仓配 H5 第一版只保留采购员、收货员、拣货员、装车员、司机、仓库主管入口。不做独立上架员，上架任务由仓库主管处理。每个角色只展示自己闭环内任务，不做跨角色混合入口。 | 仓配 H5、角色权限、页面入口 |
| 139 | P1 | 状态字典锁定 | 第一版任务状态统一收口为：待处理、处理中、待确认、已完成、已关闭、提交结果待确认。不要为第一版新增复检中、报损中、异常处理中等状态。 | 任务状态、WMS、仓配 H5 |
| 140 | P2 | 验收样例固定一套 | 第一版验收只用一套标准样例跑通：一个采购单、一个 SKU、一个批次、一个库位、一个波次、一个箱码、一条线路、一个司机、一次回仓确认。先验证闭环通，再考虑多 SKU、多批次、多线路。 | 验收用例、测试口径、闭环验证 |
| 141 | P2 | 定版后新增需求统一进二期池 | v27 定版后再出现的复检、报损报溢、异常数据分析、替换商品、温区温度、司机费用、高级筛选、离线作业等需求，统一进入二期需求池，不影响第一版验收。 | 版本边界、需求管理、后续排期 |

### 本次边界

- 第一版开始锁菜单、锁角色、锁状态、锁验收样例。
- 不做独立上架员，上架任务由仓库主管处理。
- 继续只围绕 v27 主链路和 v28 验收口径收口。
- 新需求不否定，但统一进二期池，不影响第一版定版验收。
- 不重新打开复检、报损报溢、异常数据模块、替换商品、温区温度、司机费用。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v30 第一版权限矩阵与验收串联收口拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 142 | P1 | 第一版验收按闭环串验 | 第一版验收不按单个页面孤立验收，必须从采购到仓、收货、上架、拣货、装车、出库、司机配送、回仓确认串起来验收。每个页面必须能进入下一节点或回到来源单据。 | WMS、仓配 H5、验收标准 |
| 143 | P1 | 上架任务由仓库主管处理 | 第一版上架任务由仓库主管处理，收货员只完成收货，不默认具备上架权限。仓库主管可在 WMS 或仓配 H5 查看待上架任务，并完成上架、填写库位和数量。 | 仓库主管、收货员、上架流程 |
| 144 | P1 | 角色权限矩阵冻结 | 采购员只查看采购到仓和入库结果；收货员只处理收货；拣货员只处理拣货；装车员处理装车，并在授权后确认出库；司机只处理配送和回仓交接；仓库主管处理上架、转派、关闭任务和查看全员记录。第一版不做角色自定义配置。 | WMS 权限、仓配 H5、角色菜单 |
| 145 | P2 | 历史记录保存但不做复杂报表 | 第一版保留任务历史、库存流水、操作日志、失败提交记录，并支持基础条件查询。不做复杂统计报表、绩效排名、异常分析。 | 历史记录、库存流水、操作日志 |
| 146 | P2 | UI 文案统一按作业动作命名 | 第一版按钮和状态文案统一使用确认收货、确认上架、完成拣货、确认装车、确认出库、回仓确认、关闭任务等作业动作，避免提交、处理、完成等泛词混用。 | WMS 文案、仓配 H5 文案、验收口径 |

### 本次边界

- 继续不扩第一版范围，只收口权限矩阵、验收串联和文案口径。
- 不做独立上架员，上架由仓库主管处理。
- 不做自定义角色配置、复杂报表、绩效排名、异常分析。
- 不重新打开复检、报损报溢、异常数据模块、替换商品、温区温度、司机费用。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v31 第一版单据追踪与库存流水收口拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 147 | P1 | 来源单据链必须不断 | 第一版单据链固定为：采购单 → 入库单 → 上架任务 → 波次单 → 拣货任务 → 箱码 → 装车单 → 出库单 → 司机配送单 → 回仓确认单 → 库存流水。每个任务详情必须展示来源单据和当前单据，不允许出现无来源孤立任务。 | WMS、仓配 H5、单据追踪 |
| 148 | P1 | 箱码作为交接凭证 | 拣货、装车、出库、司机交接、回仓确认都围绕箱码追踪。扫码失败可以手工录入，但必须记录操作人、操作时间和原因。 | 箱码、装车、司机交接、回仓确认 |
| 149 | P1 | 库存流水口径冻结 | 入库、上架、波次锁定、取消锁定、确认出库、回仓确认都必须生成库存流水。库存流水必须带来源单据、SKU、批次、库位、数量、操作人、操作时间。 | 库存流水、库存状态、操作留痕 |
| 150 | P2 | 差异原因字典收口 | 第一版差异原因只保留基础原因：实收少于采购、实收多于采购、拣货缺货、装车少箱、司机带回、扫码失败手输。只记录原因，不进入异常数据模块。 | 差异记录、原因字典、验收口径 |
| 151 | P2 | 任务详情必须能查历史 | 每个任务详情要能查看本任务的操作历史和关联库存流水。第一版不做导出、不做复杂报表、不做异常分析。 | 任务详情、历史记录、库存流水 |

### 本次边界

- 第一版所有任务必须有来源单据，不允许无来源孤立任务。
- 箱码作为拣货、装车、出库、司机交接、回仓确认的交接凭证。
- 库存流水只记录库存变化和来源，不承载金额结算。
- 不做无来源手工库存调整。
- 不做异常数据池、报损报溢审批、复杂对账报表。
- 不把仓库页面扩展成结算页面。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。

## 2026-06-23 v32 第一版任务关闭与失败提交收口拍板

| 编号 | 优先级 | 调整项 | 拍板结论 | 影响范围 |
| --- | --- | --- | --- | --- |
| 152 | P1 | 任务不能删除，只能关闭 | 第一版所有仓库任务生成后不允许物理删除。确实无法继续执行时，由仓库主管关闭任务，并记录关闭原因、操作人、操作时间和来源单据。 | WMS、仓配 H5、任务状态 |
| 153 | P1 | 关闭任务不自动改库存 | 关闭任务只终止当前作业流，不自动产生库存变化。库存变化必须来自入库、上架、锁定、取消锁定、确认出库、回仓确认等明确动作。 | 任务关闭、库存流水、库存状态 |
| 154 | P1 | 弱网失败提交要有待确认状态 | H5 提交失败或超时后，不允许一线人员反复盲点提交。页面进入提交结果待确认，系统查到结果后再回写成功或失败。 | 仓配 H5、弱网重试、状态回写 |
| 155 | P2 | 手工补录只做兜底 | 扫码失败、设备异常时允许手工录入箱码或单号，但必须记录原因、操作人和操作时间。手工录入不等于异常模块，也不触发财务或订单处理。 | 箱码录入、单号录入、操作留痕 |
| 156 | P2 | 已完成任务不允许直接改结果 | 已完成的收货、上架、拣货、装车、出库、回仓确认结果不允许直接改原记录。需要修正时只追加更正记录，或由仓库主管发起新的处理动作，并保留原始记录。 | 历史记录、主管处理、审计追踪 |

### 本次边界

- 第一版任务不允许物理删除，只能由仓库主管关闭并留痕。
- 关闭任务不自动改变库存，库存变化只能来自明确库存动作。
- 弱网失败提交进入提交结果待确认，不做复杂工单流程。
- 手工补录只作为扫码或设备异常的兜底，不触发财务、结算、订单处理。
- 已完成任务不直接改原记录，只追加更正记录或发起新的处理动作。
- 不做异常数据模块、报损报溢、库存任意手工调账。
- 本次只追加拍板记录，不修改 UI 原型和功能清单。
